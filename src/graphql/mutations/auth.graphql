mutation Login($input: LoginInput!) {
  login(input: $input) {
    access_token
    refresh_token
    user {
      id
      email
      firstName
      lastName
      role {
        id
        name
      }
    }
  }
}

mutation Logout($refreshToken: String!) {
  logout(refreshToken: $refreshToken)
}
